import { App } from 'vue';
import { default as RetroGrid } from './components/RetroGrid.vue';
import { ThemePreset, ThemeConfig, RetroGridProps } from './types';
export { RetroGrid };
export type { ThemePreset, ThemeConfig, RetroGridProps };
declare const _default: {
    install: (app: App) => void;
};
export default _default;
